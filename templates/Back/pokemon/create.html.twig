{% extends 'base_back.html.twig' %}

{% block title %}Hello PokemonController!{% endblock %}

{% block body %}
    <a href="{{ path('back_pokemon') }}">Back</a>
    <h1>Create Pokemon</h1>
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit">Create</button>
    {{ form_end(form) }}
{% endblock %}
